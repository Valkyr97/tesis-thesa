export enum entity {
  CATEGORY = 'category',
  DEVELOPER = 'developer',
  GAME = 'game',
  HEADLINE = 'headline',
  TEAM = 'team',
}

export enum HeadlineType {
  EVENT = 'event',
  NEW = 'new',
}

export enum QuestionType {
  CHOICE = 'CHOICE',
  TEXT = 'TEXT',
  SCALE = 'SCALE',
  DATE = 'DATE',
  TIME = 'TIME',
}
